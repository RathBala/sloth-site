# PostHog (marketing site)

This static landing page uses the [PostHog JavaScript web SDK](https://posthog.com/docs/libraries/js) for explicit marketing-site events only.

## Configuration

### Environment variables (recommended)

The browser reads `window.__SLOTH_POSTHOG__` from a **generated** file, [`src/assets/js/posthog-config.js`](../src/assets/js/posthog-config.js) (gitignored). Do not edit that file by hand.

1. Copy [`.env.example`](../.env.example) to **`.env`** in the repo root.
2. Set:

   - **`POSTHOG_API_KEY`**: your PostHog project API key (`phc_…`). Required for events to send.
   - **`POSTHOG_API_HOST`**: optional; defaults to **`https://eu.i.posthog.com`** (EU, same as `npx @posthog/wizard --region eu`). Use `https://us.i.posthog.com` for US Cloud if needed.

3. Run **`yarn posthog:config`** (also runs automatically before **`yarn dev`** and **`yarn build`**).

On **Netlify**, add the same variables under Site settings → Environment variables (no committed `.env` required). The build command runs `yarn build`, which generates `posthog-config.js` before compiling CSS.

### Why not commit the API key?

The PostHog **project key used in the browser is not a secret**—anyone can see it in DevTools or the loaded JS. Using `.env` / CI env vars still helps you **avoid committing keys to git**, use **different projects per environment**, and **rotate keys** without editing HTML.

If `POSTHOG_API_KEY` is empty or invalid, PostHog does not initialize and a console warning is logged (`[posthog-analytics]`).

## Where code lives

| Piece                                  | Location                                                                                                                                                               |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Env → `window.__SLOTH_POSTHOG__`       | Generated by [`scripts/generate-posthog-config.mjs`](../scripts/generate-posthog-config.mjs) → [`src/assets/js/posthog-config.js`](../src/assets/js/posthog-config.js) |
| Loader stub + `array.js` bootstrap     | Inline script in [`src/index.html`](../src/index.html)                                                                                                                 |
| Init, shared properties, funnel events | [`src/assets/js/posthog-analytics.js`](../src/assets/js/posthog-analytics.js)                                                                                          |

## Tracking posture

PostHog is configured for lightweight explicit telemetry:

- `autocapture: false`
- `capture_pageview: false`
- `persistence: 'memory'`
- no cross-subdomain cookie stitching
- session recording explicitly disabled on the marketing site

The site sends only the named events below. `disable_session_recording: true` keeps the marketing site out of replay even though the shared PostHog project records privacy-masked sessions on `budget.slothmoney.app`. The provider's authorized replay domain is also limited to the budget app. CTA URL attribution still forwards campaign context to the budget app through query parameters, but PostHog does not create a shared cookie identity across `slothmoney.app` and `budget.slothmoney.app`.

## Custom events (funnel)

| Event          | When                                                                                  |
| -------------- | ------------------------------------------------------------------------------------- |
| `landing_view` | Once, after PostHog loads on the initial page view                                    |
| `cta_clicked`  | User clicks a tracked CTA (`<a data-analytics-cta="...">` to `budget.slothmoney.app`) |

CTA placements use `data-analytics-cta` values: `header-sign-in`, `header-start`, `mobile-menu-start`, `mobile-menu-sign-in`, `hero-start`, `hero-sign-in`, `pricing-start`, `final-cta-start`. `pricing-start` is retained as the historical placement identifier for the free-beta access block so the funnel remains comparable.

`cta_clicked` also includes `cta_placement` and `cta_label` (trimmed link text).

## App handoff (query string)

On page load, [`src/assets/js/posthog-analytics.js`](../src/assets/js/posthog-analytics.js) rewrites each tracked CTA link to `https://budget.slothmoney.app` so the budget app receives:

| Param                                                               | Purpose                                                                                       |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `utm_*`, `creative_id`, `pain_angle`, `audience`, `landing_variant` | Copied from the **current landing URL** when missing on the CTA (preserves campaign context). |
| `entry_point`                                                       | The `data-analytics-cta` value (e.g. `hero-start`).                                           |
| `intent`                                                            | `signin` when the placement id contains `sign-in`; otherwise `signup` (primary CTAs).         |
| `cta_id`                                                            | Same as `entry_point` for downstream funnels.                                                 |

This runs **even when PostHog is disabled** (missing API key) so attribution still reaches the app. The budget app stores first-touch values in `sessionStorage` — see its `docs/features/posthog-analytics.md`.

## Properties on every event

These are merged onto captured explicit events via `before_send` and `register`:

| Property                                                   | Source                                                   |
| ---------------------------------------------------------- | -------------------------------------------------------- |
| `utm_source`, `utm_medium`, `utm_campaign`, `utm_content`  | Current page URL query string                            |
| `creative_id`, `pain_angle`, `audience`, `landing_variant` | Current page URL query string                            |
| `device_type`                                              | Derived in the browser: `mobile`, `tablet`, or `desktop` |

Missing query parameters are sent as empty strings.

## Local verification

1. Put a valid `POSTHOG_API_KEY` in `.env` and run `yarn posthog:config` (or `yarn dev`, which runs it for you).
2. Run `yarn dev` and open the site with test query params, e.g.  
   `?utm_source=test&utm_medium=paid&utm_campaign=spring&utm_content=hero&creative_id=a1&pain_angle=time&audience=couples&landing_variant=v1`
3. In PostHog, confirm `landing_view` and `cta_clicked` include the properties above.

## Related

- [Tracking inventory](./tracking-inventory.md) - cross-provider source of truth for marketing-site analytics, visit alerts, disabled providers, and privacy posture.
- [Meta Pixel](./meta-pixel.md) - disabled until paid acquisition needs marketing consent.
- [Paid social tracking runbook](./paid-social-tracking.md) - paused while Meta Pixel is disabled.
- Google Analytics is disabled; PostHog is the active analytics integration.
- [`src/visitor-tracker.js`](../src/visitor-tracker.js) posts to a Netlify function for internal visit alerts; it is independent of PostHog.
